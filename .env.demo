# --- Google Cloud Platform (GCP) ---
GCP_PROJECT_ID=some-demo-name
GCP_REGION=global
GOOGLE_APPLICATION_CREDENTIALS=/some/route/some-file.json

# --- Gemini Model & Live API ---
VERTEX_MODEL_NAME=gemini-3-flash-preview
GOOGLE_GENAI_USE_VERTEXAI=true

# --- Audio Settings (Linux/PulseAudio) ---
# Device index for input microphone; leave blank to use system default
AUDIO_DEVICE_INDEX=0

# --- Operational Controls ---
LOG_LEVEL=INFO
ENABLE_VOLUME_BAR=1

# --- Browser Configuration ---
BRAVE_BINARY=/usr/bin/brave-browser
BRAVE_USER_DATA_DIR=/home/YOUR_USER/.config/BraveSoftware/Brave-Browser
BRAVE_PROFILE=Profile 2

# --- Network ---
WS_PORT=8765

# --- Voice & Tuning ---
WAKE_KEY=hey_mycroft
WAKE_THRESHOLD=0.7
VAD_THRESHOLD=0.5	# Lower number => more sensitive to quiet speech.
SILENCE_SECONDS=1.0
COMMAND_TIMEOUT=3.0